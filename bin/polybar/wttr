#!/usr/bin/env bash
set -euo pipefail

format="%C %t"

location="$1"
if [ "$#" -ge 1 ] && [ -n $1 ]; then
    location=$1
fi

weather=$(curl --fail -sG wttr.in/$location --data-urlencode "format=$format")
echo -n $weather
