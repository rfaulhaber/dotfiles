#+title: audit work queue
#+startup: overview

Work queue from the [2026-08-04 Tue] repo audit (multi-agent review: 12 area
reviewers + adversarial verification of every removal/security/bug claim;
items below marked /verified/ survived an independent refutation attempt).

How to work this file: sections are ordered by urgency — drain =Now= first,
then work each section top to bottom. Within a section, =[#A]= before =[#B]=
before =[#C]=. Check off prune sub-items with ~C-c C-c~. The =Slices= section
below cuts across all of them: it says which items can run concurrently and
which share files, for when you want to fan work out rather than drain
top-down.

* TODO Slices [/]
Parallelization map over the outstanding items, cut [2026-08-12 Wed] from a
re-verification of the tree at =fa6774e=. Task state lives on the items
themselves — these headings are a routing view, not a second queue.

Landed work: =audit/eval-fixes-and-oci-cleanup= was fast-forwarded into
=main= and pushed [2026-08-13 Thu]. The B1/B2/B4/D1 batch (plus the sops
store-leak fix, sure sidecar pins, lscr normalization, GPU-suffix updater,
per-system deploy checks) is committed directly on local =main=, *unpushed* —
pushing fires =build-and-cache.yml= on the self-hosted runners.

#+begin_example
now, in parallel:   A1 · B1 · B4 · D2(decide) · C1
then:               B3 (after B1)          C2 (after C1)
then:               B2                     C3 (after C2)
then:               D1 (after B2)
#+end_example

Only two cross-track file collisions exist: =flake.nix= (B1 → B3) and the host
=configuration.nix= files (B2 → D1). The C chain is ordered by shared files,
not by logic — see the partition note under C3.

** DONE A1 — rpi5 installer eval  /no deploy/
CLOSED: [2026-08-12 Wed 12:14]
Item:** TODO /Fix rpi5-installer eval/ (=Now=). The last live =[#A]=.

Files:** TODO =nix/images/rpi5-installer.nix=, ideally factoring the graft at
=nix/hosts/prometheus/configuration.nix:130-147=** TODO into a shared module.
Verify** TODO with ~nix eval .#rpi5-installer.drvPath~. Collides with nothing.

** DONE B1 — Zero-risk deletions  /no deploy/
CLOSED: [2026-08-13 Thu 11:11]
- Landed [2026-08-13]: roc bootloader package + flake output, =mesa.nix=,
  and =migrate-datasets.nu= together with its SKILL.md section, all deleted.
  =bin/nix-lines-history.nu= KEPT by decision. Verified against the
  post-sops-fix baseline: no host toplevel moved — including atlas, whose
  host dir lost =migrate-datasets.nu=, which is the sops fix doing its job.
Items:** TODO =Prune= → judgment-call removals (roc bootloader,
=bin/nix-lines-history.nu=);** TODO parked overlays (=mesa.nix=); migration
artifacts** TODO (=migrate-datasets.nu=).

Files:** TODO =nix/pkgs/roc-rk3328-cc-bootloader/= + =flake.nix:223=,
=bin/nix-lines-history.nu=,** TODO =nix/overlays/mesa.nix=,
=nix/hosts/atlas/migrate-datasets.nu=** TODO *together with* its section in
=config/claude/skills/draft-oci-service/SKILL.md=** TODO (they are one unit), and
=CLAUDE.md:55,141=.** TODO Own every =CLAUDE.md= edit here so no other slice has to
touch** TODO that file. Collides with B3 on =flake.nix=.

** DONE B2 — Host-touching prune  /hyperion switch + vulcan deploy/
CLOSED: [2026-08-13 Thu 11:11]
- Landed [2026-08-13]: kitty module deleted (hyperion was the last enabler;
  nushell's ~use_kitty_protocol~ kept — that's the graphics protocol, not the
  program), vulcan =enp5s0= stanza dropped. Exactly hyperion and vulcan
  toplevels moved. Staged only — hyperion needs a switch, vulcan a deploy.
Items:** TODO =Prune= → kitty module; vulcan =enp5s0= stanza.

Files:** TODO =nix/modules/programs/kitty/=,
=nix/hosts/hyperion/configuration.nix:49=,
=nix/hosts/vulcan/configuration.nix:165-170=.** TODO The vulcan half is
networking-only** TODO against an already-active =lan0=. Collides with D1 on both
host** TODO files.

** DONE B3 — flake hygiene  /hyperion rebuild/
CLOSED: [2026-08-12 Wed 13:02]
- checks scoped; waybar follows landed (verified by full build, −1037 MiB on
  hyperion); swww closed as won't-fix with a follow-up item for =pkgs.awww=.
  No other flake input moved. Ran before B1, so the =flake.nix= collision
  B1 was warned about now lands the other way: B1 must rebase on this.
Items:** TODO =Consistency= → restrict ~checks~ to declared systems; ~follows~ for
waybar** TODO and swww.

Files:** TODO =flake.nix:33,54,214=. The ~follows~ change collapses two independent
nixpkgs** TODO evals and rebuilds both packages on hyperion. Collides with B1.

** DONE B4 — generated/ tree decision  /no deploy/
CLOSED: [2026-08-13 Thu 11:11]
- Decided [2026-08-13]: KEEP. The tree is the config export for hosts /not/
  managed by nix — consumers are external machines, which is why no in-repo
  reference exists. Documented in =generated/README.org= (+ a CLAUDE.md
  line) with the regeneration command so no future audit re-flags it.
Item:** TODO =Consistency= → generated/ tree has no consumer, no CI enforcement.

Confirmed** TODO [2026-08-12]: *zero* references to =generated/= from any =.nix=,
=.nu=,** TODO or workflow file; 11 checked-in files under =generated/{linux,darwin}/=.
Pure** TODO decision plus a delete-or-wire. Independent of everything.

** TODO C1 — OCI core plumbing  /fleet deploy, highest risk/
Items:** TODO =Bugs= → ~mkNetworkService~ create-once; =Consistency= →
secrets-rotation** TODO restart path.

Files:** TODO =nix/modules/linux/oci/default.nix:17= and =:121-133= — same file, and
the** TODO same class of defect: declared intent that never reconciles against live
state.** TODO Riskiest slice in the queue: network recreation can drop container
connectivity** TODO mid-deploy, and a failed activation re-applies the old spec onto
already-mutated** TODO live state. Deploy hosts one at a time with a raised
~--confirm-timeout~.

** TODO C2 — OCI exposure surface  /fleet deploy/
Item:** TODO =Consistency= → standardize ~bindAddress~, retire placebo
~openFirewall~.

Spread** TODO re-counted [2026-08-12]: *11* modules expose ~openFirewall~
(=immich-ml=,** TODO =jellyfin=, =grafana=, =plex=, =netbird=, =open-webui=, =loki=,
=prometheus=,** TODO =podman-exporter=, =pangolin=, =wolf=) against *5* exposing
~bindAddress~** TODO (=netbird=, =pangolin=, =pocket-id=, =podman-exporter=,
=wolf-den=).

** STRT C3 — OCI service-module cleanup  /per-service deploys/
- State "STRT"       from "TODO"       [2026-08-12 Wed 13:02]
- Landed [2026-08-12]: the three small prescribed fixes (immich/immich-ml
  ~mkGpuImage~ dedupe, transmission ~dependsOn~, prowlarr → lscr.io). All
  verified rendering no-ops; only atlas's toplevel moved, and only by the
  prowlarr image string.
- Still open: the postgres/redis sidecar factoring. Blocked on decisions —
  and note the recon found *five different PG majors* (14/16/17/18/18)
  verified against on-disk =PG_VERSION=, so normalizing them would destroy
  databases; the refactor has to preserve them per-service. Fix sure's
  unpinned sidecars first (see the item filed under =Bugs & footguns=).
- Update [2026-08-13]: sure's sidecars are pinned (done), and the per-service
  upstream research is complete — findings recorded on the factoring item
  under =Consistency & architecture=. Every major we run is in-support
  upstream, so the factoring absorbs no forced migrations. Only the helper
  design decision remains.
Items:** TODO =Consistency= → postgres/redis sidecar factoring; immich/immich-ml
GPU-suffix** TODO dedupe; transmission ~dependsOn~ + prowlarr → lscr.io.

Postgres** TODO sidecar blocks confirmed near-verbatim across =immich=, =sure=,
=forgejo=,** TODO =vikunja=, =miniflux=, =paperless=.

Partition** TODO note for the C track: C1 owns =default.nix=, C2 owns =lib.nix= plus
the** TODO exposure options, C3 owns individual service modules. Agree that split up
front** TODO and all three can run concurrently; without it, C2 and C3 both edit the
same** TODO service files and C2's helper additions collide with C3's factoring.

** DONE D1 — Shared ~nix.settings~ baseline  /fleet deploy, nix.conf only/
CLOSED: [2026-08-13 Thu 11:11]
- Landed [2026-08-13] per decision: only the hosts that build closures carry
  substituters — hyperion (deploys) and vulcan/prometheus (CI) enable
  ~modules.nix.substituters.enable~, which wires one union list in =conf.nix=
  minus the host's own harmonia. atlas/pallas/hecate dropped their lists
  (janus never had one); the niri/emacs modules lost their per-module cachix
  additions since the baseline covers the only hosts that built those
  closures. The bigHosts hostname if-chain became ~modules.nix.bigHost~.
  Staged only — this is the fleet-wide nix.conf deploy wave.
- Found while verifying: the =niri-flake= input injects
  =niri-epireyn.cachix.org= + =cache.nixos.org= into *every* host's
  substituters (janus included, before and after this change). Pre-existing,
  out of scope here, but it means "janus declares none" never meant "janus
  has none".
Item:** TODO =Consistency= → shared ~nix.settings~ baseline.

Confirmed** TODO [2026-08-12]: every host duplicates substituters/trusted-public-keys
*except** TODO janus, which declares none at all*. Also replaces =conf.nix='s
~download-buffer-size~** TODO hostname if-chain with a host option. Wide but shallow
—** TODO nix.conf only. Collides with B2 on the hyperion and vulcan host files.

** STRT D2 — CI trust boundary  /decision first/
- State "STRT"       from "TODO"       [2026-08-12 Wed 18:20]
- Decided and executed [2026-08-12]: PORT, don't delete. =flake-update.yml=
  and =oci-update.yml= now exist under =.github/workflows/=, and
  =update-oci-digests.nu= moved to =bin/= since it is also a hand-run tool.
  The =.forgejo= tree is left in place until each ported workflow has run
  green once.
- Recon corrected the premise twice. (1) Those workflows ran on
  *codeberg.org/ryf/dotfiles*, not =git.home.lan=; they stopped because the
  codeberg runner units were deleted (=ed48b3a= Jul 28, =8ba0f08= Aug 7), not
  because a forge disabled Actions. (2) The flake-update pipeline was *also*
  independently broken: reports crossed jobs via the host-local =/ci-state=
  bind mount while the aarch64 legs run on prometheus and finalize on vulcan,
  so the =f44cad9= completeness gate hard-failed on three "missing" hosts
  every run. Artifacts fix that by construction.
- Still open, and still the actual security question: the =forgejo-runner= on
  vulcan and prometheus is *actively running CI for =ryan/trading-bot=*, and
  its nix-daemon socket grant looks load-bearing there — one job container was
  named =JOB-nix-build= running =ubuntu:latest=, which ships no nix. Read
  trading-bot's own workflows before touching =containerOptions=.
- BLOCKER for the ported workflows, owner-only: GitHub Settings → Actions →
  General → *Allow GitHub Actions to create and approve pull requests* must be
  enabled, or =POST /pulls= 403s regardless of the ~permissions:~ block. The
  failure shape is bad: the branch pushes fine, then PR creation dies, leaving
  an orphan branch every run.
Item:** TODO =Security hardening= → vulcan CI nix-daemon socket + rootful podman.

Decide** TODO before coding: is the forgejo runner against =git.home.lan= still doing
work** TODO post-GitHub-migration? If it is retired, deleting it dissolves the finding
and** TODO a module with it. If it stays, the fix is the one its own comment names —
drop** TODO the nix-daemon socket from ~containerOptions~/~validVolumes~, or front the
podman** TODO socket with a restricting proxy (=modules.services.docker-socket-proxy=
already** TODO runs on vulcan, but GET/HEAD-only; a runner needs write).

* TODO Now [6/6] :urgent:
:PROPERTIES:
:LOGGING:  DONE(!) [X](!) STRT(!) [-](!) logdone logstart
:COOKIE_DATA: todo recursive
:END:
** DONE [#A] Fix prometheus eval: kernel passthru graft missing ~buildDTBs~
CLOSED: [2026-08-12 Wed 10:47]
- State "DONE"       from "HOLD"       [2026-08-12 Wed 10:47]
- Done: fixed by =6bba78a=; the graft at
  =nix/hosts/prometheus/configuration.nix:130-147= now carries both ~target~
  (with a fallback for nixpkgs =b7c2ada= no longer elaborating ~linux-kernel~
  on the host platform) and ~buildDTBs~. Re-verified [2026-08-12 Wed]:
  ~nixosConfigurations.prometheus.config.system.build.toplevel.drvPath~
  evaluates. The image half of the same breakage is split out below.
=nix/hosts/prometheus/configuration.nix:116-127= grafts only ~target~ onto the
pinned Pi 5 vendor kernel's passthru; current nixpkgs' device-tree module also
reads ~buildDTBs~. Fails at committed HEAD (verified in clean worktree):
~nixosConfigurations.prometheus~ won't evaluate and =nix flake check= fails
repo-wide.
** DONE [#A] Fix rpi5-installer eval: image never got the ~buildDTBs~ graft
CLOSED: [2026-08-12 Wed 12:14]
- State "DONE"       from "TODO"       [2026-08-12 Wed 12:14]
- Done: graft lifted verbatim out of the prometheus host file into
  =nix/modules/hardware/rpi5-vendor-kernel.nix=, imported by both consumers.
  Deliberately *not* added to =nix/modules/default.nix='s import list: it sets
  ~boot.kernelPackages~ unconditionally and needs the ~nixos-raspberrypi~
  specialArg, so it stays opt-in and is reached by relative path — the same
  way the images already reach =../modules/ssh/keys.nix=. No =flake.nix=
  change was needed; both call sites already pass the specialArg.
- Verified: ~nix eval .#rpi5-installer.drvPath~ now succeeds. All seven
  NixOS hosts still evaluate; eos still evaluates; atlas's kernel is
  untouched stock =linux-6.18.44=, confirming the module did not leak.
- The lift is a *provable* no-op for prometheus. Its toplevel drv hash moved
  (=1v6kndwn…= → =ljv307dy…=) but the only differing inputs are
  =activate.drv= / =dry-activate.drv=, and inside =activate= exactly one line
  differs: the sops manifest path. Derivation hashes are recursive, so every
  other input being bit-identical proves the kernel and all boot config are
  unchanged. See the note below for why the manifest moved at all.
- Side finding (new item filed under =Bugs & footguns=): the manifest moved
  because =modules/programs/sops/default.nix:51= sets
  ~defaultSopsFile = "${hostDir}/secrets.yaml"~, string-interpolating the
  whole host directory into the store. Any edit to any file under
  =nix/hosts/<host>/= therefore rehashes that host's toplevel.
The graft lives in the prometheus *host* config, but =nix/images/rpi5-installer.nix=
is a separate ~nixosSystem~ that pulls the vendor kernel directly, so it still
dies at HEAD (verified [2026-08-12 Wed], ~nix eval .#rpi5-installer.drvPath~):

#+begin_example
error: attribute 'buildDTBs' missing
at «github:nixos/nixpkgs/2fcb964»/nixos/modules/hardware/device-tree.nix:126:19
#+end_example

This is what the installers item under =Security hardening= was waiting on —
the ~PasswordAuthentication = false~ change is already in tree for all three
images and only this eval blocks confirming the rpi5 one. Prefer factoring the
passthru graft into a module both consumers import over copying it, so the two
can't drift again.
** DONE [#A] Decide niri overlay before next hyperion switch
CLOSED: [2026-08-04 Tue 16:41]
- State "DONE"       from "TODO"       [2026-08-04 Tue 16:41]
- Done: removed the overlay and niri rebuilds without error
Uncommitted =flake.lock= bump + =68c4891= (overlay unwired): the pinned niri
input's Cargo.lock still locks =libdisplay-info-sys 0.3.0= against nixpkgs
libdisplay-info 0.4.0 — the overlay's own ~overlayNeeded~ predicate is true, so
a hyperion rebuild may fail. Overlay is self-disabling; re-wiring it is
harmless. Dry-build hyperion toplevel to settle it.
** DONE [#A] newt: drop or gate the rootful podman socket mount [5/5]
CLOSED: [2026-08-05 Wed 18:02]
- State "DONE"       from "STRT"       [2026-08-05 Wed 18:02]
- State "STRT"       from "TODO"       [2026-08-05 Wed 12:38]
=nix/modules/linux/oci/newt.nix:70-72= unconditionally mounts
=/run/podman/podman.sock= (the =:ro= flag is decorative on a unix socket;
image runs uid 0). Pangolin's control plane on janus transitively gets root on
atlas and vulcan. If newt doesn't need docker integration, remove the mount;
else make it opt-in / socket-proxy it.

Newt source (v1.15.0) confirms the integration is three read-only calls —
=GET /containers/json=, =GET /containers/{id}/json=, =GET /events= — and that
an unset socket degrades silently (~available: false~), so the tunnel never
depended on it. Note it enumerates *every* container regardless of the
~networks~ list: ~DOCKER_ENFORCE_NETWORK_VALIDATION~ defaults off.
- [X] gate the mount behind ~dockerSocket~ (default =null=, scheme-asserted);
  atlas/vulcan now pass =unix:///var/run/docker.sock= explicitly, so the
  root-equivalence is visible in the host config instead of implied by
  ~enable = true~
- [X] tecnativa (=ghcr.io/tecnativa/docker-socket-proxy=) container route
  built, smoke-tested against the real socket, then *retired before deploy*.
  Reliability review killed it: thin maintainer bench ([[https://github.com/Tecnativa/docker-socket-proxy/issues/66][#66]] unanswered since
  2022, [[https://github.com/Tecnativa/docker-socket-proxy/issues/180][#180]] — v0.5.0 hangs =GET /vX.Y/version=, newt's exact negotiation
  call — stalled), and released tags are never rebuilt, so the =v0.4.2= pin
  (=haproxy:3.2.4-alpine=, [2025-12-16]) accumulates base CVEs indefinitely
  while =update-oci-digests.nu= (digest-refresh only) can't help. Design
  review also found the TCP endpoint reachable cross-network by IP under the
  repo-wide ~isolate = "false"~, with =GET /containers/{id}/json= exposing
  ~Config.Env~ — every container's sops-rendered secrets. A first deploy
  attempt [2026-08-05 Wed 12:58] also surfaced an aardvark-dns startup race
  against the proxy's network alias. All three problems are structural to the
  containerized-TCP design and vanish with the unix-socket module below.
- [X] replaced by native ~modules.services.docker-socket-proxy~
  (=nix/modules/linux/services/docker-socket-proxy/=): nixpkgs haproxy as a
  hardened host service (DynamicUser, =AF_UNIX=-only, seccomp, no caps) on
  =/run/docker-socket-proxy/docker.sock=; GET/HEAD only, =_ping=/=version=
  always allowed, per-section allows via ~allowedApiSections~. No image, no
  registry pull at deploy time, patched with every flake bump. Functionally
  tested through haproxy 3.4 with a mock backend: 14-case ACL matrix incl.
  =/v1.51/version= (the #180 shape) and both =/events= forms routing to the
  =timeout server 0= backend; the test caught haproxy ≥3.3 rejecting
  same-name frontend/backend before it could fail on the host.
- [X] atlas: cutover *deployed and verified* [2026-08-05 Wed 17:28], gen 146.
  Proxy active (=NRestarts=0=, no ALERTs, only the expected timeout warning),
  socket =srw-rw---- docker-socket-proxy= at the expected path, newt's sole
  bind is the filtered socket, 6 networks, tunnel established 2s after start,
  httplog shows newt's enumeration (=HEAD /_ping=, =GET /v1.44/containers/json=,
  ~40 inspects, all 200) and the ACL probe (=GET /networks= → 403,
  =GET /_ping= → 200). No tecnativa container or =socket-proxy= network
  remains; ~systemctl --failed~ clean. Design notes: newt mounts
  =/var/run/docker.sock= (~hostSocket~ + ~dockerSocket = unix://~), ordered
  ~After=~ the proxy only — no ~Requires=~; the picker is optional, the
  tunnel is not. Pangolin add-resource picker confirmed working post-deploy.
  (Minor: no organic =/events= hit seen in the first minutes — likely lazy;
  spot-check httplog for it eventually.)

  Three failed deploy attempts, two mechanisms, all rolled back cleanly to
  gen 145 (tunnel up throughout):
  1. [2026-08-05 Wed 12:58] and [2026-08-05 Wed 17:03] — default
     ~--confirm-timeout 30~ expired while the bundled nixpkgs bump
     (=26.11.20260802= → =26.11.20260804=) restarted every podman unit.
     Land the flake bump first, or deploy with ~-- -s --confirm-timeout 180~.
  2. [2026-08-05 Wed 17:05] (with =180=) — real unit failure: haproxy 3.4's
     unconditional QUIC startup self-test (~quic_test_socketopts~) opens an
     IP socket, which ~RestrictAddressFamilies=AF_UNIX~ turned into a fatal
     =Address family not supported by protocol=; podman-newt then failed on
     the missing mount source. =no-quic= is not a keyword this build accepts.
     Fixed: allow =AF_INET/AF_INET6= + ~IPAddressDeny=any~ (no-network moves
     from the syscall layer to the packet layer). Reproduced and re-validated
     locally under the exact sandbox via ~systemd-run~ — proxy stays active,
     full ACL matrix passes.
- [X] vulcan: cutover *deployed and verified* [2026-08-05 Wed 17:54],
  gen 74. Proxy active (=NRestarts=0=, no ALERTs), socket 660
  DynamicUser-owned, newt's sole bind is the filtered socket, tunnel up,
  httplog shows the enumeration (13× 200s) and the ACL probes (=/_ping= 200,
  =/networks= 403). forgejo-runner / wolf / podman-exporter confirmed still
  on the real socket as intended; ~systemctl --failed~ clean. Switch-window
  noise, all unrelated: newt→Pangolin token auth 403'd ~2m before
  succeeding (control-plane side), one clean netbird restart (package bump),
  transient determinate-nixd FlakeHub auth chatter.

Residual curiosity, both hosts: newt has not opened =GET /events= despite
the event monitor being wired — enumeration is list+inspect only so far.
Picker works regardless; if start/stop freshness ever looks stale in the
picker, check the proxy httplog for =/events= before suspecting the ACLs.
** DONE [#A] navidrome: remove ~ND_EXTAUTH_TRUSTEDSOURCES = "0.0.0.0/0"~
CLOSED: [2026-08-05 Wed 11:40]
- State "DONE"       from "TODO"       [2026-08-05 Wed 11:40]
=nix/modules/linux/oci/navidrome.nix:70= — upstream default *disables* header
auth; this default enables it for everyone while the port publishes on all
interfaces. PoC: ~curl -H 'Remote-User: <user>' '.../rest/ping.view?v=1.16.1&c=x'~.
Scope to the reverse-proxy subnet or drop it.
** DONE [#A] crowdsec: bouncer key rotation arms a delayed site-wide 403
CLOSED: [2026-08-06 Thu 12:21]
- State "DONE"       from "TODO"       [2026-08-06 Thu 12:21]
- Done: ExecStartPost now re-registers every enabled bouncer on each start
=nix/modules/linux/oci/crowdsec.nix:130= — rotating =crowdsec/bouncer-api-key=
updates Traefik's copy but LAPI keeps the old registration (entrypoint only
runs ~cscli bouncers add~ when absent; =crowdsec.db= persists). Plugin serves
from cache until expiry, then 403s the world. Fix: ExecStartPre re-registers
the key (delete + add), or document rotation as a two-step.

Implemented as ExecStartPost, not ExecStartPre — ~cscli~ has to run inside
the container, which doesn't exist until ExecStart (same reason
~disabledHubItems~ hooks post-start). The new =crowdsec-sync-bouncer-keys=
hook polls for cscli readiness, then unconditionally ~delete~ + ~add~s each
enabled bouncer with the key from the container's own env
(=$BOUNCER_KEY_<name>= expanded inside ~podman exec sh -c~, so the value
never hits a host command line or the journal). Delete+add window is
milliseconds against the plugin's 60s stream-poll + cache, so no user-facing
blip. Verified by building janus's rendered =podman-crowdsec.service= unit
and inspecting the generated script.

Rotation is still a two-step (documented in the ~bouncers~ option): sops
value rotations restart nothing (restartTriggers hash placeholder names, see
the arch-section item), so after deploy both =podman-crowdsec= and
=podman-traefik= need a restart — the sync hook then converges LAPI to the
new key.

* TODO Security hardening [11/12] :security:
** TODO [#B] vulcan CI: reconsider nix-daemon socket + rootful podman in job containers
=nix/hosts/vulcan/oci.nix:144= hands every job container the host nix-daemon
socket; jobs already run as host root via the rootful podman socket
(=forgejo-runner.nix:141,149=) and root is a nix trusted-user. Anything that
can trigger a workflow can plant store paths deploys later consume.

Scope narrowed [2026-08-12]: this is now *only* about the residual
~forgejo-runner~ against =git.home.lan= (=nix/hosts/vulcan/oci.nix:133-160=;
the socket mount moved to =:154-157=). The native GitHub runners that replaced
the build workload are already the safe model — ~DynamicUser~, ephemeral, and
registered under ~allowed-users~ *never* ~trusted-users~
(=nix/modules/linux/services/github-runner/default.nix:69,80-81=). Retiring
the forgejo runner would close this outright; see slice D2.
** DONE [#B] node_exporter: scope ~openFirewall~ to an interface
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: module no longer forwards ~openFirewall~ upstream; the allow renders
  under ~firewall.interfaces.<iface>~ and an assertion rejects ~openFirewall~
  without ~interface~. Hosts pinned: atlas =eno2=, vulcan =lan0=, pallas
  =end0=, hecate =enu1u1u1=, prometheus =end0=; janus keeps its wt0
  narrowing (openFirewall stays false). Verified by eval: pallas 9100 only
  under =interfaces.end0=, absent from the global list.
=nix/modules/linux/services/observability-agent/default.nix:154= forwards to
upstream's unscoped rule (all interfaces, both families); pallas/hecate carry
global-unicast IPv6 on the WAN-facing iface. Every sibling module scopes via
~cfg.interface~ — do the same here.
** DONE [#B] gerbil: drop ~CAP_SYS_MODULE~
CLOSED: [2026-08-06 Thu 15:48]
- State "DONE"       from "STRT"       [2026-08-06 Thu 15:48]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deployed janus gen 64; ~CapAdd=["CAP_NET_ADMIN"]~ only, wg0 up,
  tunnel traffic live seconds after restart.
- Note: staged (deploy wave 2): cap dropped, host now preloads wireguard via
  the pangolin module's ~boot.kernelModules~; gerbil 1.4.3 source confirmed
  netlink-only (no modprobe path, no userspace fallback). Awaiting janus
  deploy.
=nix/modules/linux/oci/pangolin.nix:487= — with the cap added, podman's
seccomp *allows* ~init_module~; a memory bug in the public-ingress netns
becomes remote→host-root. Test with only ~NET_ADMIN~.
** DONE [#B] traefik: disable ~api.insecure~ dashboard on :8080
CLOSED: [2026-08-06 Thu 15:48]
- State "DONE"       from "STRT"       [2026-08-06 Thu 15:48]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deployed janus gen 64; in-network curl to :8080 now refused,
  ingress routing unaffected (dashboard 200 via gerbil netns).
- Note: staged (deploy wave 1): api block removed; confirmed in source at our
  pins that nothing consumes traefik's API (badger is in-process; pangolin
  serves config TO traefik); rendered YAML re-validated. Awaiting janus
  deploy.
=nix/modules/linux/oci/pangolin.nix:50-52= — full unauthenticated API in
gerbil's netns, reachable from every container on the pangolin network and
from newt sites over the tunnel.
** DONE [#B] netbird: restrict coturn UDP range + TrustedPeers
CLOSED: [2026-08-06 Thu 15:48]
- State "DONE"       from "STRT"       [2026-08-06 Thu 15:48]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deployed (coturn range in gen 62, rest gen 64); turnserver.conf
  =min-port=49152 max-port=49351=, firewall matches, no 65535 rule remains.
- Note: staged (deploy wave 1): relay range now 49152-49351 via module
  defaults (=turnserver.conf= + firewall derive from the same options);
  ~TrustedPeers~ =127.0.0.1/32= (nothing proxies for mgmt; empty list is a
  trust-all trap, documented in a comment); ~TURNConfig.Secret~ literal
  replaced with ~"unused"~ + why-comment (~TimeBasedCredentials = false~,
  coturn validates static lt-cred-mech). Awaiting janus deploy. Live janus
  triage [2026-08-06]: zero TURN allocations observed.
=nix/modules/linux/oci/netbird.nix:494= opens UDP 49152-65535 on janus's
public IP (upstream compose default). Also =:50=: set
~ReverseProxy.TrustedPeers~ to an explicit subnet — NOTE upstream treats an
*empty* list as 0.0.0.0/0, so empty is a silent no-op. Tidy the literal
~TURNConfig.Secret = "secret"~ (inert while TimeBasedCredentials is on, but
misleading).
** DONE [#B] ssh: turn off ~ForwardAgent~ for janus (review fleet-wide habit)
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: janus + janus-netbird blocks no longer set it, falling back to the
  ~*~ block's ~ForwardAgent = false~; signing-oracle rationale left as a
  comment. Fleet-wide habit reviewed: the LAN hosts keep it deliberately
  (agent use from trusted boxes); janus was the only untrusted-ingress
  host.
- Follow-up [2026-08-07 Fri]: the removal also broke deploy-rs — passwordless
  sudo on janus was ~pam_ssh_agent_auth~ challenging the forwarded agent, and
  the account has no password there. Restored with a *scoped* agent instead
  of the fleet agent: =~/.ssh/id_janus_deploy= is authorized only on janus,
  held by the ~janus-deploy-agent~ user unit on hyperion
  (~deployAgent.enable~ in =nix/modules/ssh/client.nix=), and the janus
  blocks set ~ForwardAgent~ to that agent's socket. Root on janus hijacking
  the socket can only sign with a key whose entire authority is login/sudo
  on janus itself — zero fleet oracle value. One-time bootstrap: ~-A~ /
  ~-o ForwardAgent=yes~ does *not* override the config block's explicit
  socket path (verified empirically — the scoped agent still gets
  forwarded), so bootstrap instead by ~ssh-add~-ing =id_host= into the
  scoped agent's socket, deploying, then restarting
  ~janus-deploy-agent~ to drop it.
=nix/modules/ssh/client.nix= sets it for *every* host block; janus is the one
that terminates untrusted internet traffic — root there can use the forwarded
agent as a signing oracle against the whole fleet.
** DONE [#B] installers: disable ssh password auth
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: all three images set ~PasswordAuthentication = false~ +
  ~KbdInteractiveAuthentication = false~; ~initialPassword~ stays for
  console bootstrap (physical access only). x86 ISO and rpi3 image eval
  clean; rpi5 image eval is broken at committed HEAD by the same external
  nixos-raspberrypi ~buildDTBs~ issue as prometheus — change is staged for
  when that clears.
=nix/images/*.nix= ship ~initialPassword = "nixos"~ (public in this repo) with
sshd on; x86 image also has passwordless wheel sudo. Set
~PasswordAuthentication = false~ or use a real hash.
** DONE [#B] atlas: set ~bindAddress~ on podman-exporter
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: ~bindAddress = "127.0.0.1"~ (prometheus scrapes via the
  observability-network alias; the host publish was unused) and the wrong
  "no LAN exposure" comment replaced. Loopback addresses are now exempt
  from the module's ~ip_nonlocal_bind~ sysctl so this doesn't loosen it
  host-wide; janus's overlay-IP case still sets it (verified by eval:
  atlas absent, janus =1=, ports =127.0.0.1:9882:9882=). Bonus: dropped
  ~virtualisation.podman.dockerSocket.enable~ from the module —
  =/run/podman/podman.sock= ships with ~virtualisation.podman.enable~
  alone; the option only added a host-wide =/run/docker.sock= alias that
  nothing in the fleet consumes (newt uses the socket-proxy/native path,
  forgejo-runner mounts the native path).
=nix/hosts/atlas/oci.nix:402-407= binds 0.0.0.0:9882 while the comment claims
no exposure; ~openFirewall = false~ can't gate a podman DNAT. One-liner:
~bindAddress = "127.0.0.1"~ (prometheus scrapes same-host).
** DONE [#C] keepalived: rotate VRRP password, decide sops-vs-accept
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: accepted as-is by decision [2026-08-06] — the value functions as a
  peer-sync token, not a secret (VRRPv2 PASS auth is cleartext in every
  advert; the auth feature was dropped in VRRPv3 as security theater). No
  rotation; documented with comments at both hosts' ~authPass~.
Literal ~authPass = "0x7gMrmq"~ at =nix/hosts/{pallas,hecate}/configuration.nix:83=
in a public repo. VRRPv2 PASS auth is cleartext link-local anyway — rotate,
then either accept in-repo as non-secret or drop auth entirely.
** DONE [#C] pihole: trim ~NET_ADMIN/NET_RAW/SYS_TIME~ on the DHCP-less backup (hecate)
CLOSED: [2026-08-06 Thu 17:20]
- State "DONE"       from "TODO"       [2026-08-06 Thu 17:20]
- Done: module now grants ~NET_ADMIN~+~NET_RAW~ only when ~dhcp.enable~
  (they must travel together — upstream ground truth from FTL v6.7 source:
  the entrypoint checks only NET_ADMIN, and FTL missing NET_RAW keeps the
  web UI alive with DNS+DHCP silently dead; upstream's README omits
  NET_RAW because docker's default caps include it — podman's don't).
  ~SYS_TIME~ moved behind a new ~ntpSync~ option, default off, with
  ~FTLCONF_ntp_sync_active~ kept in lockstep: it exists solely for FTL's
  NTP *client* steering the host clock, which systemd-timesyncd already
  owns on these hosts. Net: hecate drops all three caps; pallas keeps the
  DHCP pair and sheds SYS_TIME.
** DONE [#C] grafana: OIDC ~rootUrl~ is plain http — cookie/code cross LAN cleartext (=nix/hosts/atlas/oci.nix:413=)
CLOSED: [2026-08-06 Thu 17:20]
- State "DONE"       from "TODO"       [2026-08-06 Thu 17:20]
- Done (decision: local TLS on the LAN name): caddy module grew a per-proxy
  ~tls = "internal"~ option (caddy's built-in local CA, persisted under
  =baseDir/data=) + an assertion tying it to ~scheme = "https"~; pallas
  serves =https://grafana.home.lan= with auto http→308 redirect; atlas
  ~rootUrl~ is https and the grafana module now sets
  ~GF_SECURITY_COOKIE_SECURE~ whenever the rootUrl is https. Also fixed a
  latent trap: =podman-caddy= had no restartTriggers on the Caddyfile
  content (bind mount pins the store inode), so config-only changes never
  activated on deploy.
- Manual steps after deploy: (1) update the PocketID client's redirect URI
  to the https form; (2) trust pallas's caddy root cert on devices —
  =/docker/caddy/data/caddy/pki/authorities/local/root.crt=.
- Residual, accepted for now: TLS terminates on pallas; the
  caddy→atlas:3000 hop is still plain http on the LAN (noted in a comment
  at the proxy entry). Full fix would need caddy/TLS on atlas or an
  encrypted upstream.
** DONE [#C] atlas NFS ~no_root_squash~ / samba ~map to guest~ — review whether still intended
CLOSED: [2026-08-06 Thu 17:20]
- State "DONE"       from "TODO"       [2026-08-06 Thu 17:20]
- Done: read-only investigation on vulcan+atlas found NOTHING writes as
  root — jellyfin/plex write as uid 1000, ollama as 994 (native unit,
  DynamicUser), and atlas on-disk ownership corroborates (0 uid-0 files in
  =tv= and =llm-models= full-tree; one untouched root-owned fossil from
  2023 in =movies=). Switched all three exports to ~root_squash~: free
  hardening that specifically blocks the ephemeral-CI-job-as-root vector
  (the one consumer class that couldn't be statically ruled out). samba
  ~map to guest = "Bad User"~ is conditional on the deliberately-guestOk
  roms share — intended, left as-is.

* TODO Bugs & footguns [19/20] :bug:
** DONE [#B] sure: postgres + redis sidecars run on floating tags, unpinned
CLOSED: [2026-08-13 Thu 11:11]
- Done: both pinned in =atlas/oci-images.json= to the *index digests atlas is
  running right now* (classified via podman inspect on the host + skopeo
  media-type checks), in their own commit, before any factoring. postgres's
  =16-alpine= tag still resolves to the same index, so that pin is doubly a
  no-op; redis's =8-alpine= has moved upstream — that bump now arrives
  through the weekly digest PR instead of silently on the next recreate.
Found [2026-08-12] while scoping the postgres factoring. Every other db/cache
sidecar in the fleet has a real-digest entry; sure's two do not appear in
=nix/hosts/atlas/oci-images.json= at all, so they are running by floating tag
on atlas *right now* and are also outside the weekly digest-refresh — meaning
deferring to CI does nothing for them. This violates the repo's own
auto-update-by-default convention. Pin both to the digests they are currently
running (a true no-op for what atlas pulls), in their own commit, *before* any
refactor touches those modules — so the factoring's no-op proof runs against
an already-pinned baseline.
** DONE [#C] paperless/sure: ~networks~ option declared but never applied
CLOSED: [2026-08-13 Thu 11:11]
- Done (decision: delete). Nuance found on the way out: the option wasn't
  fully inert — it /created/ the named networks, it just attached no
  container to them, so setting it produced orphan networks and no actual
  re-homing. The dedicated per-service network is now created
  unconditionally, which is exactly what the default rendered; atlas's
  toplevel stayed byte-identical.
Found [2026-08-12]. Both modules declare a ~networks~ option that is wired to
no container and no unit. Zero live impact (atlas sets neither), but it is a
silent no-op option that reads as working. Either wire it through or delete
it. Deliberately kept out of the postgres-factoring slice: wiring it would
change the rendered =--network= flags and contaminate that slice's
drvPath-equality check.
** DONE [#C] sops: ~defaultSopsFile~ interpolates the whole host dir into the store
CLOSED: [2026-08-13 Thu 11:11]
- Done: path arithmetic (~hostDir + "/secrets.yaml"~) on *both* leaks — the
  ~defaultSopsFile~ and the ~pathExists~ assertion, which this item had
  missed and which copied the dir all the same. Only =secrets.yaml= enters
  the store now (=/nix/store/…-secrets.yaml=). One-time fleet-wide toplevel
  rehash, then proven empirically: deleting =migrate-datasets.nu= from
  atlas's host dir left atlas's toplevel byte-identical. Grep confirms no
  other module interpolates ~hostDir~.
Found [2026-08-12] while proving the rpi5 graft lift was a no-op.
=nix/modules/programs/sops/default.nix:51= sets
~defaultSopsFile = "${hostDir}/secrets.yaml"~. String-interpolating a path
copies the *entire* =nix/hosts/<host>/= directory into the store (observed as
=/nix/store/…-prometheus/secrets.yaml=), so editing *any* file in a host's
directory — a comment in =configuration.nix=, an unrelated =oci.nix= tweak —
rehashes that host's sops manifest, which rehashes =activate.drv=, which
rehashes the toplevel. That is a cache miss on every host-local edit, and it
makes "did my change affect this system?" unanswerable by drv comparison.
Fix candidate: ~builtins.path { path = …; name = …; }~ on the single file, or
a path literal, so only =secrets.yaml= enters the store. Same family as the
already-fixed toplevel source-path leak; check whether other modules
interpolate ~hostDir~ the same way.
** TODO [#C] oci: ~mkNetworkService~ is create-once — subnet/gateway/isolate edits are silent no-ops on existing hosts; reconcile or document
** DONE [#B] job-ops: LLM key env-var mapping wrong for openai/gemini/glm
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: openai/gemini/glm now map to =LLM_API_KEY=, openrouter keeps
  =OPENROUTER_API_KEY= (upstream v0.9.0 contract); sops wiring untouched.
=nix/modules/linux/oci/job-ops.nix:235= writes =OPENAI_API_KEY= etc.; upstream
v0.9.0 only reads =LLM_API_KEY= (universal) and =OPENROUTER_API_KEY=
(openrouter). Those providers currently get no credentials.
** DONE [#B] netbird: ExecStartPre clobbers ~DataStoreEncryptionKey~ every start
CLOSED: [2026-08-06 Thu 15:48]
- State "DONE"       from "STRT"       [2026-08-06 Thu 15:48]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deployed janus gen 64; first-ever start with NO "generating a new
  key" journal line, live config hash-matches the sops render, zero decrypt
  errors since deploy.
- Note: staged (deploy wave 2): 32-byte base64 key minted into janus sops
  (=netbird/data-store-encryption-key=) and rendered via placeholder, making
  the ~cp -f~ idempotent. Confirmed at v0.76.1: empty key → generate+persist
  each start (8 regenerations since Jul 13 in the live journal); encrypted
  fields are user email/name + invites, not setup keys/PATs. Post-deploy:
  restart =podman-netbird-mgmt=, watch for decrypt errors, delete stuck user
  rows if any.
=nix/modules/linux/oci/netbird.nix:446= — template renders the key empty;
~cp -f~ overwrites the rw-mounted =management.json= each start; upstream then
generates a fresh key, invalidating what the old one encrypted. Render once /
preserve the generated key.
** DONE [#B] ~_managedPaths~ registers baseDir but mounts subdirs nothing creates
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: caddy =data=/=config= and pihole =etc-pihole= now created by
  ExecStartPre ~mkdir~ (unconditional — works on non-ZFS pallas/hecate).
  Reviewer noted a latent zfs-ordering nuance if a zfs host ever adopts these
  services (no such host today).
=nix/modules/linux/oci/caddy.nix:237= (=baseDir/{data,config}=) and pihole
(=etc-pihole=). Works on existing hosts only because dirs were hand-made;
fresh install/restore of pallas/hecate fails silently. Register the subdirs or
tmpfiles them.
** DONE [#B] crowdsec: mount log *directory* not file; add logrotate for traefik access.log
CLOSED: [2026-08-06 Thu 15:48]
- State "DONE"       from "STRT"       [2026-08-06 Thu 15:48]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deployed janus gen 64; dir mount live, acquisition reading 972/972
  lines, logrotate-checkconf succeeded, copytruncate stanza rendered.
  Pre-Nix stale ymls quarantined out of the log dir before deploy.
- Note: staged (deploy wave 1): acquisitions now mount ~dirOf(hostPath)~ so
  recreation/rotation can't blind detection; weekly copytruncate logrotate
  (rotate 8, compress) added in =pangolin.nix= for the access log (47MB
  unrotated since May 29 on janus). Awaiting janus deploy.
=crowdsec.nix:33-36= pins the inode — any recreation silently blinds
detection. No rotation exists for the janus access log (latent disk-fill).
** DONE [#B] zfs: ~requestEncryptionCredentials~ ignores ~encryptedDatasets~
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: now false when the module owns encrypted datasets — atlas true→false
  (per-dataset sops-gated units are sole owners), hyperion/vulcan unchanged;
  adversarially reviewed, comment corrected.
=nix/modules/linux/services/zfs/default.nix:113= — always true today, so
upstream's pool-wide load-key path runs alongside the module's dedicated
per-dataset units on atlas. Self-documented TODO in the module.
** DONE [#B] CI: merge-reports.nu must fail on missing matrix legs
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: expected hosts now single-sourced from =.forgejo/hosts.json= feeding
  both workflows' dynamic matrices; missing report or missing =hosts.json= is
  a hard failure.
=.forgejo/scripts/merge-reports.nu= globs whatever =build-report-*.json= exist
— a leg that dies pre-report vanishes and the run still reads as success.
Cross-check against the expected host list.
** DONE [#C] jellyfin: ~tvDir~/~moviesDir~ → ~types.nullOr types.str~ (currently ~str~ + ~default = null~, throws when forced)
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
** DONE [#C] firefox module: remove dead ~modules.programs.zsh~ reference (=firefox/default.nix:32=)
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
** DONE [#C] recyclarr: wire ~cfg.instances~ or delete it (+ dead ~sonarrUrl~/~radarrUrl~, same null-default trap) (=recyclarr.nix:54,270,282=)
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: deleted dead ~instances~/~sonarrUrl~/~radarrUrl~ + orphaned
  ~instanceOpts~; no host set them.
** DONE [#C] darwin dock: ~optionalAttrs~ in a list yields ~{}~ when ghostty disabled — attrTag eval error (=dock.nix:25=); use ~optionals~
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: ~lib.optionals~; both ghostty branches eval on eos.
** DONE [#C] wayland module: move NVIDIA env vars to the nvidia module (=wayland/default.nix:22-24=, verbatim dupe of =nvidia/default.nix:41-43=)
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: wayland copy removed; nvidia module is sole owner.
** DONE [#C] lib: ~hostnameFromPath~ regex is =[[:alpha:]]+= only — digits/hyphens in a hostname return null
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: =[[:alnum:]-]=; all current hostnames re-verified.
** DONE [#C] options.nix: ~types.attrs~ alias shims shallow-merge — same top-level key from two modules silently clobbers; document or type properly
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: documented at the shim declarations (typing change deliberately not
  taken).
** DONE [#C] extract-embedded-subs: oneshot exits 0 even when every extraction fails
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: missing media root is now fatal (unmounted-dataset case);
  all-planned-failed exits 1; partial failures stderr-only so one corrupt
  file can't pin the unit failed and pollute the ~systemctl --failed~ deploy
  signal.
** DONE [#C] check-nixpkgs-stale.nu: unauthenticated GitHub API call gates the flake-update pipeline (rate-limit flakiness)
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: optional token via workflow secret; API failures classified; gate
  semantics preserved.
** DONE [#C] podman-exporter: ~bindAddress~ flips ~ip_nonlocal_bind~ host-wide; forgejo default sshPort collides with gerbil TCP port
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- Done: loopback bindAddresses no longer set the sysctl (127.0.0.0/8 is
  always bindable); janus's overlay-IP case still does, by design. The
  3402 "collision" assessed as intentional pairing, not a bug: gerbil's
  ~tcpPort~ relays the same forgejo-ssh flow end-to-end
  (git.3679.space:3402 → gerbil → tunnel → atlas forgejo sshPort 3402), so
  matching defaults keep the port stable across the path. Only a real
  conflict if forgejo and gerbil ever co-host — left as-is.

* DONE Prune [8/8] :prune:
CLOSED: [2026-08-13 Thu 11:11]
** DONE [#C] Judgment-call removals [4/4]
CLOSED: [2026-08-13 Thu 11:11]
- [X] =roc-rk3328-cc-bootloader= package (no host targets that board) —
  deleted [2026-08-13], with its =flake.nix= output and CLAUDE.md line.
- [X] kitty module: one reviewer claims nothing launches it — sanity-check
  before removing. Probe [2026-08-06]: found nothing launches it
  (niri/env/scripts all clear) — removal is safe if intended. Scope correction
  [2026-08-12]: hyperion is the *only* enabler now
  (=nix/hosts/hyperion/configuration.nix:49=); vulcan has already dropped it,
  so this is a one-host change. Deleted [2026-08-13].
- [X] =bin/nix-lines-history.nu= — KEPT by decision [2026-08-13]; stays as a
  manual tool.
- [X] vulcan transitional =enp5s0= NIC stanza
  (now =nix/hosts/vulcan/configuration.nix:165-170= — own comment says
  removable post-reboot; verify reboot happened). Probe [2026-08-06]: confirms
  reboot happened, =lan0= active, nothing references =enp5s0= — removable.
  Update [2026-08-12]: the =store= extraPools re-add this item flagged has
  already landed (=configuration.nix:153=, ~extraPools = ["zroot" "store"]~);
  only the =enp5s0= stanza itself is left. Deleted [2026-08-13].
** DONE [#B] Delete obsolete parked overlays [6/6]
CLOSED: [2026-08-13 Thu 11:11]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
Verified against pinned nixpkgs =e72e4f2= (see =nix/overlays/README.org= for
the conventions):
- [X] =firefox_devedition_20260713.nix= (nixpkgs at 154.0b4, Hydra-cached;
  re-enabling would downgrade)
- [X] =netbird.nix= (nixpkgs at 0.76.1 on stock buildGoModule)
- [X] =pam_ssh_agent_auth.nix= (fixed upstream via ~LD=$(CC)~, issue closed)
- [X] KEEP =niri_20260728.nix= — still needed (see Now section)
- [X] =mesa.nix=: test stock 26.1.6, delete if it holds — probe [2026-08-06]
  says stock 26.1.6 holds on hyperion (no gpu/drm journal errors) — deleted
  [2026-08-13].
- [X] =awesome.nix=: delete unless AwesomeWM is coming back
** DONE [#B] Remove finished-business migration artifacts [3/3]
CLOSED: [2026-08-13 Thu 11:11]
- State "STRT"       from "TODO"       [2026-08-06 Thu 13:44]
- [X] =nix/hosts/atlas/zfs-conversion/= (superseded by =disko-zfs.nix=;
  phase2.nix no longer evaluates)
- [X] =nix/hosts/atlas/migrate-datasets.nu= (references a doc never committed)
  — deleted [2026-08-13] together with its SKILL.md section, as one unit.
- [X] =nix/hosts/janus/{capture,restore}-docker-state.nu= (docker→podman
  cutover done; references nonexistent CUTOVER.org)
** DONE [#B] Remove retired host dirs [5/5]
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
Verified: no flake entry, zero references outside their own dirs, no
=.sops.yaml= keys.
- [X] =nix/hosts/helios/=
- [X] =nix/hosts/hestia/=
- [X] =nix/hosts/nexus/=
- [X] =nix/hosts/nike/=
- [X] retired-host leftovers: =doom.d/hosts/{helios,orange,ponos}.el= deleted.
  The nushell files =config/nushell/hosts/{config,env}/{ponos,wsl}.nu= were
  KEPT — audit claim refuted [2026-08-06]: they are statically match-dispatched
  from =config/nushell/{config,env}.nu=, and nushell's static =source= means
  deleting them breaks parsing of config.nu itself on every host. Retire them
  only together with their match arms.
** DONE [#B] Remove unused modules [3/3]
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- [X] =nix/modules/linux/services/mullvad/= (no host enables; gluetun's
  "mullvad" refs are its own ~vpnProvider~; drop import in
  =services/default.nix:10=)
- [X] =nix/modules/programs/tmux/= (zellij is the multiplexer in use)
- [X] ~mkRaspberryPiNixOSHost~ in =nix/lib/nixos.nix:63= (zero call sites;
  also fix CLAUDE.md which still advertises it)
** DONE [#B] age/ragenix: strip dead agenix machinery
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: module deleted outright (its rage package was already provided by the
  sops module; hyperion's enable removed); ragenix input + lock entry gone;
  orphaned =secrets.nix= files and pallas =passwd.age= removed.
~modules.programs.age.agenix~ enabled by zero hosts, yet the module
unconditionally imports =inputs.ragenix.nixosModules.default= into every host
incl. eos. hyperion only wants the package. Reduce to a package install (or
delete) and drop the =ragenix= flake input. Includes deleting orphaned
=nix/hosts/{pallas,hecate}/secrets/secrets.nix= (hecate's =passwd.age= doesn't
even exist).
** DONE [#B] Remove dead config/ dirs [7/7]
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
Verified: no wholesale =config/= symlink exists; modules reference subdirs
individually; none of these are referenced anywhere in =nix/=.
- [X] =config/hammerspoon/=
- [X] =config/wezterm/=
- [X] =config/hypr/=
- [X] =config/sway/=
- [X] =config/tofi/=
- [X] =config/awesome/= (only "ref" is the parked awesome-git overlay)
- [X] =config/ghostty/{ponos,darwin}= static files (module generates config
  from Nix)
** DONE [#C] Micro dead code [4/4]
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- [X] ~modules.services.zfs.shares~ option (declared, never consumed)
- [X] ~printing.printers~ option (hardcoded list ignores it)
- [X] gpg module's unused ~isWayland~ let-binding
- [X] niri window rule for WezTerm (=window-rules.nix:2=)

* TODO Consistency & architecture [10/14] :arch:
** TODO [#B] Standardize ~bindAddress~ across OCI modules; retire placebo ~openFirewall~
Ten modules expose ~openFirewall~ (toggles INPUT-chain rules podman DNAT never
traverses); only five expose ~bindAddress~ (the control that works). Add
~bindAddress~ to the shared helper, default =127.0.0.1= on overlay-reached
hosts, make ~openFirewall~ honest or remove it.
** TODO [#B] Secrets-rotation restart path
~restartTriggers~ hash template *content* = placeholder names, not values
(=oci/default.nix:129=, comment overclaims). Value rotations restart nothing —
root cause behind the crowdsec trap, the netbird clobber staying hidden, and a
past gluetun/AirVPN debugging session. Options: sops-nix restart hooks, or
hash the rendered file at activation. Fix the comment regardless.
** TODO [#B] Factor postgres/redis sidecar boilerplate into =oci/lib.nix=
Near-verbatim across immich/paperless/vikunja/linkding (+ friends):
~mkPostgresService~ with image pin, ordering, recordsize-aware
~_managedPaths~. Would also end copy-paste drift (transmission missing
~dependsOn~, prowlarr on ghcr vs lscr).

Upstream research [2026-08-13] (each project's own source, per the
read-upstream-source rule), as design input for the factoring:
- *immich*: our =14-vectorchord0.4.3-pgvectors0.2.0= pin is upstream
  v3.1.0's /exact/ reference compose pin, digest-identical; code floor
  ~>=14~, VectorChord ~>=0.3 <2~; sidecar repo is immich's own
  =ghcr.io/immich-app/postgres=. Valkey 9 matches upstream. No forced
  migration signaled; the v3.0.0 pgvecto.rs drop predates us.
- *paperless*: PG 18 is upstream's literal compose pin. redis 8 declared
  compatible, but upstream's shipped default switched to
  =valkey/valkey:9-alpine= [2026-06-18] — stay-or-follow is a real choice.
- *miniflux*: floor PG 11 (raised for a PG18 FIPS fix — PG18 is an exercised
  target), no ceiling; dev compose runs =postgres:latest=. 18 is fine.
- *forgejo 16.0.2*: docs + pgx driver floor ~>=14~, no ceiling; upstream CI
  only tests PG 16, so our 17.2 is permitted-but-untested there. Keep.
- *vikunja*: our module is deliberately SQLite-only — upstream-sanctioned at
  this scale ("more than a handful of users" is their postgres threshold).
  No sidecar exists, so it drops out of the factoring entirely. If PG ever:
  floor 12+, their example uses postgres:18. Redis optional, not needed.
- *sure*: postgres:16 matches upstream compose verbatim; pgvector only
  matters if AI/RAG with Anthropic gets enabled (degrades gracefully);
  upstream floats redis unpinned.
Net: all five majors in production (14/16/17.2/18/18) are in-support; the
helper needs a *mandatory* per-service version argument and must not default
one. Sidecar naming, recordsize-aware ~_managedPaths~, and the dedicated
network stay per-service. Design sign-off still pending.
** DONE [#B] Shared ~nix.settings~ baseline
CLOSED: [2026-08-13 Thu 11:11]
- Done — full record on the D1 slice entry. Builders-only substituters via
  ~modules.nix.substituters.enable~ (hyperion, vulcan, prometheus); other
  hosts dropped their lists per decision; bigHosts if-chain became
  ~modules.nix.bigHost~. Staged, not deployed.
Substituters/trusted-public-keys duplicated verbatim on every host *except
janus, which has none* (likely accidental — janus builds pull nothing from
the home cache). Extract a module; also replace =conf.nix='s
~download-buffer-size~ hostname if-chain with a host option.
** DONE [#C] flake: restrict ~checks~ to declared systems (currently maps all four deploy-rs systems)
CLOSED: [2026-08-12 Wed 13:02]
- State "DONE"       from "TODO"       [2026-08-12 Wed 13:02]
- Done: ~lib.genAttrs~ over ~top.config.systems~, with a ~lib.filter~ guard so
  the expression stays total if ~systems~ later gains an entry deploy-rs
  doesn't cover. ~nix eval .#checks --apply builtins.attrNames~ now returns
  =["aarch64-darwin" "x86_64-linux"]=; ~nix flake check --no-build~ passes.
- Consequence to be aware of: =checks.aarch64-linux= is now gone, so running
  ~nix flake check~ *on* pallas/hecate/prometheus finds no deploy checks for
  its own system. Bounded — nothing in =.github/= or =.forgejo/= runs
  ~nix flake check~, and =.github/hosts.json= drives eval by toplevel instead.
  Add =aarch64-linux= to the filter if you'd rather keep it.
- Not fixed (separate hazard, still armed): on eos ~nix flake check~ matches
  =aarch64-darwin=, which is a declared system, so it would still try to
  realize the Linux toplevels on a Mac. Restricting to declared systems
  cannot fix that; only a literal ~checks.x86_64-linux = …~ would.
** DONE [#C] flake: add ~follows~ for =waybar= and =swww= (each drags an independent nixpkgs eval)
CLOSED: [2026-08-12 Wed 13:02]
- State "DONE"       from "TODO"       [2026-08-12 Wed 13:02]
- Done (waybar): ~inputs.nixpkgs.follows~ added; verified by a full successful
  waybar build, and it drops 346 store paths / 1037 MiB of waybar-exclusive
  closure from hyperion. Lock diff audited against committed HEAD: the *only*
  changes are the removal of the =nixpkgs_11= node and waybar's input edge
  re-pointing at the root nixpkgs. No other input's rev or narHash moved, so
  this is not a bundled flake bump.
- Won't-fix (swww): the follows was tested and *fails to build twice over* —
  first the old rust-overlay pin against new nixpkgs, then, with rust-overlay
  bumped, swww 0.11.2's vendored waybackend-scanner 0.7.0 rejecting the
  ~frozen=~ attribute added in wayland-protocols 1.49. The functional line is
  untouched; a comment now records the constraint.
- Follow-up filed below: upstream renamed to =awww= and moved to Codeberg;
  =pkgs.awww= is the real fix and is strictly better than a follows.
** DONE [#C] swww → ~pkgs.awww~: drop the flake input entirely
CLOSED: [2026-08-12 Wed 17:46]
- State "DONE"       from "TODO"       [2026-08-12 Wed 17:46]
Fallout from the swww follows being unbuildable [2026-08-12]. Upstream moved
to =codeberg.org/LGFae/awww=; the GitHub repo is frozen at 0.11.2 and cannot
build against wayland-protocols ≥ 1.49. nixpkgs already carries =awww= 0.12.1,
cached, which removes the input and its private nixpkgs eval outright — a
strictly better outcome than the follows would have been. Binaries renamed to
=awww= / =awww-daemon=, so this touches
=nix/modules/linux/services/desktop/swww/default.nix=, =…/wayland/default.nix:30=,
=…/random-wallpaper.nix= and =bin/random-wallpaper.nu= (3 call sites), and the
systemd unit name changes from =swww.service=. Hyperion-only.
- Done: input dropped; module dir renamed to
  =nix/modules/linux/services/desktop/awww/= with the option, unit and
  assertion renamed to match. All consumers now take =pkgs.awww=, which also
  drops the dead ~inputs~/~system~ bindings from =wayland/default.nix= and
  =random-wallpaper.nix= — and, unlike the flake input, puts the package in the
  host's overlay chain.
- Lock diff audited: removes the =swww= node *and* its three private inputs
  (=flake-compat_4=, =rust-overlay_2=, =nixpkgs_10= — the second nixpkgs eval
  this item was filed to kill). Only other change is waybar's =flake-compat_5=
  renumbering into the freed slot. No other input's rev or narHash moved, so
  this is not a bundled flake bump.
- Verified: =pkgs.awww= is 0.12.1 from the repo's own pin, ships =bin/awww= and
  =bin/awww-daemon= as the units assume, and hyperion's toplevel builds.
- Took the opportunity to retire the colon-splitting output parser in
  =bin/random-wallpaper.nu=: awww 0.12.0 added ~query --json~, which swww never
  had, so ~get_outputs "wayland"~ now parses structured data. This kills the
  failure class behind the earlier silent =perDisplay= regression. The JSON is
  an object keyed by daemon namespace (default key is the empty string), not a
  flat array, so the parse is ~from json | values | flatten | get name~; awww
  emits *nothing* rather than ={}= when it knows of no outputs, hence the
  =is-empty= guard ahead of ~from json~. Verified against a stubbed =awww=
  reproducing the exact bytes =client/src/main.rs= builds — two outputs, empty
  output, and unknown-desktop all return the expected list.
- Deploy note: this is a *user* unit rename, so ~switch-to-configuration~ stops
  =swww.service= and starts =awww.service=, killing the running daemon and
  clearing the wallpaper until the next =random-wallpaper= timer tick. awww
  0.12.0 also moved its cache to =$XDG_CACHE_HOME/awww/$PKG_VERSION=, so
  ~awww restore~ has nothing to restore on first run either way.
** DONE [#C] immich/immich-ml: dedupe GPU-suffix image-render logic
CLOSED: [2026-08-12 Wed 13:02]
- State "DONE"       from "TODO"       [2026-08-12 Wed 13:02]
- Done: ~mkGpuImage~ in =oci/lib.nix=, consumed by both modules. Verified a
  rendering no-op — vulcan's toplevel drvPath is byte-identical and its
  immich-ml image still renders
  =…:release-openvino@sha256:d5561b…= (suffix before digest, which is the
  ordering that matters: the digest pins the already-suffixed manifest).
- Fixed a latent bug the dedupe exposed: ~suffixes.${gpu} or ""~ throws on
  ~gpu = null~ — ~or~ catches a *missing attribute*, not a null attribute
  selector — so the documented CPU-only configuration could not evaluate.
  Latent only because atlas sets ~machineLearning.enable = false~ and vulcan
  sets ~gpu = "intel"~. Now short-circuited with ~optionalString~; verified
  null/intel/nvidia+digest all render correctly. The old inline code had the
  identical bug, so this is a fix, not a regression.
** DONE [#C] update-oci-digests.nu has no notion of the GPU suffix
CLOSED: [2026-08-13 Thu 11:11]
- Done, with a different design than "take the same helper": the script now
  reads the host's *rendered* =oci-containers= definitions and takes each
  entry's tag from the reference whose repository+digest match — no
  per-module suffix knowledge at all. (A sibling-~gpu~ probe was considered
  and rejected: atlas's immich /server/ also has a ~gpu~ option, for device
  passthrough, whose image is not suffixed.) vulcan's inline immich-ml pin
  moved into =oci-images.json= — rendered reference and toplevel drv verified
  byte-identical — and a vulcan dry run proposes the current
  =release-openvino= digest, not =release='s.
Surfaced [2026-08-12] by the ~mkGpuImage~ dedupe, which created the natural
seam and did not use it. =.forgejo/scripts/update-oci-digests.nu:90= fetches
by ~repository:version~ with no suffix, so it would clobber vulcan's
=release-openvino= pin with the generic =release= digest. That is why
=nix/hosts/vulcan/oci.nix:44-52= carries a hand-maintained inline pin plus a
warning comment instead of a =oci-images.json= entry. Now that suffix
rendering is centralized, the updater could take the same helper. Note the
script's fate is entangled with the =.forgejo= porting decision.
** DONE [#C] linuxserver registry drift is only 7/10 normalized
CLOSED: [2026-08-13 Thu 11:11]
- Done: all three on lscr.io. Gate passed cleanly — for each of
  nzbget/plex/calibre, the old registry's =:latest=, lscr.io's =:latest=,
  and the pinned digest all resolve to the identical manifest, so no re-pin
  and zero content change; the next atlas/vulcan deploys just recreate the
  containers with new references and pull nothing.
After prowlarr moved to lscr.io [2026-08-12]: =nzbget.nix:16= is still
=ghcr.io/linuxserver/nzbget= (the identical pattern prowlarr just left), and
=plex.nix:16= / =calibre.nix:16= use bare =linuxserver/*= short names that
depend on ~unqualified-search-registries = ["docker.io"]~ — the exact path
that broke when registries.conf went v2. Normalize all three to lscr.io,
gated on the same digest-equality check prowlarr passed.
** DONE [#C] generated/ tree: no consumer, no CI enforcement — wire it up or drop it
CLOSED: [2026-08-13 Thu 11:11]
- Done (decision: keep) — the consumers are external non-nix hosts; see the
  B4 slice entry. Documented in =generated/README.org=.
** TODO [#C] sure: =latest= tag currently tracks alpha prereleases — consider =stable=
Surfaced [2026-08-13] by the sidecar research. Upstream's own
=compose.example.yml= pins =ghcr.io/we-promise/sure:stable=; their publish
workflow gives =latest= to alpha-suffixed tags and =stable= to clean semver.
Today our pinned =latest= digest resolves to =v0.7.4-alpha.4= (published
2026-08-13) while =stable= is =v0.7.3=. The digest pin means nothing changes
without a PR, but every weekly refresh will keep proposing prerelease bumps.
Decide: move the module default to =stable= (matches upstream guidance and
the repo's pin-breakage-prone-services convention), or accept tracking
alphas deliberately.
** DONE [#C] transmission: add the ~dependsOn~ option its siblings declare; prowlarr: move to lscr.io
CLOSED: [2026-08-12 Wed 13:02]
- State "DONE"       from "TODO"       [2026-08-12 Wed 13:02]
- Done (transmission): ~dependsOn~ option added and threaded through the
  ~inherit (cfg) …~ line into ~mkArrService~. Verified a rendering no-op —
  ~mkArrService~ already defaulted ~dependsOn ? []~ and no host sets it.
- Done (prowlarr): =ghcr.io/linuxserver/prowlarr= → =lscr.io/…=, digest left
  untouched *because it did not need to change*. Gate applied before the edit:
  skopeo resolved tag =develop= on both registries to the identical manifest
  =sha256:dcb53158…=, which is already what =atlas/oci-images.json:167= pins.
  So the running image content is unchanged; the next atlas deploy just
  recreates =podman-prowlarr= with a new image reference and pulls nothing.
  Had the digests differed, the change would have been an unrequested version
  bump of a live service and I'd have left it alone.

** DONE [#B] prometheus host: pin forgejo-runner in =oci-images.json=
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: real-digest =oci-images.json= added and wired; =update-oci-digests.nu=
  now records per-host eval failures as a PR warning instead of dying on
  prometheus's known-broken toplevel.
=nix/hosts/prometheus/oci.nix:9= — currently excluded from weekly digest
automation, contrary to the auto-update-by-default policy.
* DONE Docs & stale comments [2/2] :docs:
** DONE [#C] Fix wrong-security comments [4/4]
CLOSED: [2026-08-06 Thu 16:48]
- State "DONE"       from "TODO"       [2026-08-06 Thu 16:48]
- [X] =podman-exporter.nix:128,134= "no effective privilege escalation" — a
  rootful podman socket is root-equivalent; ro mount is decorative.
  Both comments now state the socket is a deliberate root-equivalent grant
  and ~:ro~ protects only the inode.
- [X] =podman-exporter.nix:97= ~dockerSocket.enable~ comment (adds host-wide
  =/run/docker.sock=, not what the comment says). Upgraded from comment fix
  to removal — the option was entirely unnecessary (see the bindAddress
  item's Done note).
- [X] =oci/default.nix:129= restartTriggers comment (see rotation item above)
  — now states the placeholder-hash limitation and the manual
  ~systemctl restart podman-<svc>~ requirement after value rotations.
- [X] =atlas/oci.nix:402= "no LAN exposure needed" on a 0.0.0.0 bind —
  replaced by the loopback-bindAddress note.
** DONE [#C] Refresh docs [5/5]
CLOSED: [2026-08-06 Thu 13:44]
- State "DONE"       from "TODO"       [2026-08-06 Thu 13:44]
- Done: CLAUDE.md also re-synced (host table incl. prometheus, scripts list
  re-derived from =bin/=, retired-host examples dropped).
- [X] CLAUDE.md: add prometheus to host table; remove ~mkRaspberryPiNixOSHost~
- [X] =.forgejo/workflows/oci-update.yml:5= says daily, cron is weekly
- [X] =nix/hosts/pallas/README.org= says Docker, runs podman
- [X] =config/README.org= highlights awesome (dead), omits what's live
- [X] =miniflux.nix=/=open-webui.nix= baseDir examples contradict
  =/data/apps= + pool-prefix conventions

* Verified non-issues (do not re-litigate)
Findings that were adversarially tested and *refuted* during the audit:
- *gluetun restarts*: systemd ≥254 propagates ~Restart=always~ crash-restarts
  to ~Requires=~ dependents (live-tested on 261); deploys recover via the root
  target re-pull. *Never add ~BindsTo=~* — tested, it strands dependents
  permanently. Residual (untested): ~10s stale-netns window during RestartSec
  — is netavark teardown fail-closed? Only matters as a VPN-leak question.
- *desktop-environment exclusivity assertion*: works as intended.
- *ssh ~sshAgentAuth~ PAM service*: is consumed; not dead config.
- *netbird management.json perms*: sops renders 0400 and ~cp~ preserves it;
  the real issue is the encryption-key clobber (see Bugs).
- =nix/overlays/= as a whole is *intentional* parked-patch staging — never a
  prune target merely for being unreferenced (see its README.org).
