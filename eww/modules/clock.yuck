; -*- mode: Lisp -*-

(defwidget clock []
  (box :orientation "h"
       clock-poll
       (revealer (cal))))

(defpoll clock-poll
    :interval "1s"
    `date "+%a, %b %d %I:%M:%S %p"`)

(defwidget cal []
  (box :orientation "v"
       (calendar)))
