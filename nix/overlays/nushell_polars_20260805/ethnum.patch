Carries the Cargo.lock bump merged upstream in nushell as 1139611e188d
(ethnum 1.5.2 -> 1.5.3), mirroring the patch NixOS/nixpkgs#546343 adds as
pkgs/by-name/nu/nushell/plugins/polars/update-ethnum.patch.

Regenerated with context (upstream's is zero-context, keyed to absolute line
numbers) so it still applies if a nushell bump shifts the lock file without
moving ethnum off 1.5.2.

--- a/Cargo.lock
+++ b/Cargo.lock
@@ -2079,9 +2079,9 @@
 
 [[package]]
 name = "ethnum"
-version = "1.5.2"
+version = "1.5.3"
 source = "registry+https://github.com/rust-lang/crates.io-index"
-checksum = "ca81e6b4777c89fd810c25a4be2b1bd93ea034fbe58e6a75216a34c6b82c539b"
+checksum = "40404c3f5f511ec4da6fe866ddf6a717c309fdbb69fbbad7b0f3edab8f2e835f"
 
 [[package]]
 name = "event-listener"
